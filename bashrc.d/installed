# Extra sources which are required for installed apps.
alias node=nodejs
if [ -f ~/.nvm/nvm.sh ] ; then
	source ~/.nvm/nvm.sh
fi

if [ -d ~/.rvm/bin ] ; then
	export PATH="$PATH:$HOME/.rvm/bin" # Add RVM to PATH for scripting
	source ~/.rvm/scripts/rvm
fi

